---
engines:
  duplication:
    enabled: true
    config:
      languages:
      - javascript
  eslint:
    enabled: true
    channel: "eslint-8"
    config:
      config: ".eslintrc.yaml"
  fixme:
    enabled: true
    checks:
      XXX:
        enabled: false
      TODO:
        enabled: false
ratings:
  paths:
  - "**.js"
exclude_paths:
- test/
