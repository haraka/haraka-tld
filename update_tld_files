#!/bin/bash
# Update Haraka TLD files

CURL=$(which curl 2>/dev/null)

if [ $? -ne 0 ]; then
    echo "Error: curl not found"
    exit 1
fi

$CURL -s -o etc/top-level-tlds https://data.iana.org/TLD/tlds-alpha-by-domain.txt
$CURL -s -o etc/two-level-tlds http://george.surbl.org/two-level-tlds
$CURL -s -o etc/three-level-tlds http://george.surbl.org/three-level-tlds
$CURL -s -o etc/extra-tlds http://rss.uribl.com/hosters/hosters.txt 
$CURL -s -o etc/public-suffix-list https://publicsuffix.org/list/effective_tld_names.dat
