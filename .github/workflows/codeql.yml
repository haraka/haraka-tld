name: CodeQL

on:
  push:
    branches: ['master', 'main']
  pull_request:
    branches: ['master', 'main']
  schedule:
    - cron: '18 7 */14 * 4'
  workflow_dispatch:

jobs:
  codeql:
    uses: haraka/.github/.github/workflows/codeql.yml@master
