name: "CodeQL"

on:
  push:
    branches: [ master ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ master ]
  schedule:
    - cron: '18 7 * * 4'

jobs:
  call-codeql:
    uses: haraka/.github/workflows/codeql.yml@master
